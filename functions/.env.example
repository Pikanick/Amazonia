# Copy this file to functions/.env (which is gitignored) and fill in a
# real Stripe secret key from https://dashboard.stripe.com/apikeys
# (use a *test* key -- sk_test_... -- unless you specifically intend to
# process real payments).
STRIPE_SECRET_KEY=sk_test_your_key_here

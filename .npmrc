# react-currency-format's peer dependency metadata still caps out at
# react ^17 even though it works fine with React 18 in practice (its
# last release predates React 18) -- without this, a plain `npm install`
# on a fresh clone fails outright with an ERESOLVE conflict.
legacy-peer-deps=true
